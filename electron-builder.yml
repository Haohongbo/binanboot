appId: com.binanboot.app
productName: Binanboot
directories:
  output: release
files:
  - out/**
  - package.json
asar: true
asarUnpack:
  - node_modules/@duckdb/node-api/**
  - node_modules/@duckdb/node-bindings/**
  - node_modules/@duckdb/node-bindings-*/**

publish:
  provider: github
  owner: Haohongbo
  repo: binanboot
  releaseType: release

win:
  target:
    - target: nsis
      arch:
        - x64
    - target: zip
      arch:
        - x64
  artifactName: ${productName}-${version}-win-${arch}.${ext}

mac:
  category: public.app-category.finance
  target:
    - target: dmg
      arch:
        - x64
        - arm64
    - target: zip
      arch:
        - x64
        - arm64
  artifactName: ${productName}-${version}-mac-${arch}.${ext}

linux:
  category: Finance
  executableName: binanboot
  maintainer: Binanboot Maintainers <noreply@binanboot.local>
  target:
    - target: AppImage
      arch:
        - x64
    - target: rpm
      arch:
        - x64
  artifactName: ${productName}-${version}-linux-${arch}.${ext}

rpm:
  compression: bzip2

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
